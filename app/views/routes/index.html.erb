<h1> Route Information </h1>
<% flash.each do |name, msg| -%>
   <%= content_tag :div, msg, class: name %>
 <% end -%>

<% @routes.each do |r| %>
  <%= link_to r.title, route_path(r) %> </br>
<% end %>
</br>

<%= link_to 'Add new route', new_route_path %> |
<%= link_to 'Go back', root_path %>
